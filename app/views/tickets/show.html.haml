%div{:id => 'ticket'}
  %h2= @ticket.title
  = link_to "Edit Ticket", [:edit, @project, @ticket]
  = link_to "Delete Tickets", [@project, @ticket], :method => :delete, :cofirm => "Are you sure you want to delete this ticket?"
  %br
  %small= "Created by #{@ticket.user.email}"
  = simple_format(@ticket.description)